apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: "${{ values.name + '-ec2' }}"
  description: EC2 instance for "${{ values.instanceName }}"
  annotations:
    github.com/project-slug: luismiguelzapata/${{ values.infraName }}
spec:
  type: compute
  owner: "${{ values.owner }}"
